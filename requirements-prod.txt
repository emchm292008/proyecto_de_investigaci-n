# Framework web
fastapi>=0.100.0
uvicorn[standard]>=0.22.0

# Configuración desde archivos .env
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# Encriptación de contraseñas con BCrypt
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.0

# Driver async para PostgreSQL (Neon)
asyncpg>=0.28.0

# ORM async para construir queries
sqlalchemy[asyncio]>=2.0.0
greenlet>=3.0.0